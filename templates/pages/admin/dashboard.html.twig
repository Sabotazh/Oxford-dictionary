{% extends 'layout/admin-base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
    <div class="row">
        <div class="col">
            <div class="card">
                <div class="card-body">
                    {{ render_chart(users_chart) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
